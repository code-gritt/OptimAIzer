[alembic]
# Path to migration scripts
script_location = alembic

# Database URL; can use environment variable DATABASE_URL
sqlalchemy.url = postgresql://neondb_owner:npg_4bDAOTSKdN0z@ep-quiet-dream-acs172xb-pooler.sa-east-1.aws.neon.tech/optimaizer-database?sslmode=require&channel_binding=require

# Optional: prepend paths to sys.path
prepend_sys_path = .

# Path separator for multiple paths (default uses OS separator)
path_separator = os

# Post-write hooks for formatting/linting (optional)
# [post_write_hooks]
# hooks = black
# black.type = console_scripts
# black.entrypoint = black
# black.options = -l 79 REVISION_SCRIPT_FILENAME

[loggers]
keys = root,sqlalchemy,alembic

[handlers]
keys = console

[formatters]
keys = generic

[logger_root]
level = WARNING
handlers = console
qualname =

[logger_sqlalchemy]
level = WARNING
handlers =
qualname = sqlalchemy.engine

[logger_alembic]
level = INFO
handlers =
qualname = alembic

[handler_console]
class = StreamHandler
args = (sys.stderr,)
level = NOTSET
formatter = generic

[formatter_generic]
format = %(levelname)-5.5s [%(name)s] %(message)s
datefmt = %H:%M:%S
